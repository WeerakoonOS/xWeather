<!DOCTYPE html>
<html>
  <head>
    <!-- header partial -->
    <% include partials/head %>
  </head>
  <body>
    <div class="topnav" id="myTopnav">
          <!-- navbar partial -->
      <% include  partials/header %>
    </div>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input id="city" name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Get Current Air Pollution Score">
        </form>
            <% if(weather !== null){ %>
            <p><%= weather %></p>
            <% } %>

            <% if(error !== null){ %>
            <p><%= error %></p>
            <% } %>
      </fieldset>
    </div>
    <div>
      <!-- footer partial -->
      <footer>
          <% include partials/footer %>
      </footer>
    </div>
  </body>
</html>